# This file tells the frrouting package which daemons to start.
#
# Entries are in the format: <daemon>=(yes|no|priority)
#   0, "no"  = disabled
#   1, "yes" = highest priority
#   2 .. 10  = lower priorities
# Read /usr/share/doc/frrouting/README.Debian for details.
#
# Sample configurations for these daemons can be found in
# /usr/share/doc/frrouting/examples/.
#
# ATTENTION:
#
# When activation a daemon at the first time, a config file, even if it is
# empty, has to be present *and* be owned by the user and group "frrouting", else
# the daemon will not be started by /etc/init.d/frr. The permissions should
# be u=rw,g=r,o=.
# When using "vtysh" such a config file is also needed. It should be owned by
# group "frrvty" and set to ug=rw,o= though. Check /etc/pam.d/frr too.
#
# The watchfrrouting daemon is always started. Per default in monitoring-only but
# that can be changed via /etc/frr/debian.conf.
<% if scope.lookupvar('zebra') %>zebra=yes<% else %>zebra=no<% end  %>
<% if scope.lookupvar('bgpd') %>bgpd=yes<% else %>bgpd=no<% end  %>
<% if scope.lookupvar('ospfd') %>ospfd=yes<% else %>ospfd=no<% end  %>
<% if scope.lookupvar('ospf6d') %>ospf6d=yes<% else %>ospf6d=no<% end  %>
<% if scope.lookupvar('ripd') %>ripd=yes<% else %>ripd=no<% end  %>
<% if scope.lookupvar('ripngd') %>ripngd=yes<% else %>ripngd=no<% end  %>
<% if scope.lookupvar('isisd') %>isisd=yes<% else %>isisd=no<% end  %>
<% if scope.lookupvar('babeld') %>babeld=yes<% else %>babeld=no<% end  %>
<% if scope.lookupvar('fabricd') %>fabricd=yes<% else %>fabricd<% end  %>
<% if scope.lookupvar('pimd') %>pimd=yes<% else %>pimd=no<% end  %>
<% if scope.lookupvar('ldpd') %>ldpd=yes<% else %>ldpd=no<% end  %>
<% if scope.lookupvar('nhrpd') %>nhrpd=yes<% else %>nhrpd=no<% end  %>
<% if scope.lookupvar('eigrpd') %>eigrpd=yes<% else %>eigrpd=no<% end  %>
<% if scope.lookupvar('sharpd') %>sharpd=yes<% else %>sharpd=no<% end  %>
<% if scope.lookupvar('pbrd') %>pbrd=yes<% else %>pbrd=no<% end  %>
